# Unused by the CaveProductions Gradle project.
# Native code is built from the repository root with the NDK CMake toolchain
# (see cmake/toolchains/android-toolchain.cmake and contrib/scripts/android.sh).
cmake_minimum_required(VERSION 3.15)
project(GAME)
