To create a new gesture, push the window "gesture". As soon as you lift the
fingers from your touch screen, a gesture file is saved (see the logs).

Convert this with some bin to header tool and load it in UI::loadGesture